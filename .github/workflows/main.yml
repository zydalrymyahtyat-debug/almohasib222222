name: بناء ملف AAB لتطبيق الدفتر الآمن 📱

on:
  push:
    branches:
      - main
      - master

permissions:
  contents: write

jobs:
  build-aab:
    name: بناء تطبيق الأندرويد (AAB)
    runs-on: ubuntu-latest

    steps:
      # جلب المشروع
      - name: Checkout Repository 📂
        uses: actions/checkout@v4

      # إعداد Node.js
      - name: Setup Node.js 🟢
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # تثبيت الاعتماديات
      - name: Install Dependencies 🛠️
        run: npm ci

      # بناء المشروع
      - name: Build Web App 🏗️
        run: npm run build

      # إعداد Java
      - name: Set up JDK 21 ☕
        uses: actions/setup-java@v4
        with:
          distribution: zulu
          java-version: "21"

      # مزامنة Capacitor
      - name: Capacitor Sync 🔄
        run: npx cap sync

      # إصلاح Gradle Wrapper
      - name: Repair Gradle Wrapper 🛠️
        run: |
          cd android
          mkdir -p gradle/wrapper
          curl -L -o gradle/wrapper/gradle-wrapper.jar \
          https://raw.githubusercontent.com/gradle/gradle/v8.14.3/gradle/wrapper/gradle-wrapper.jar

      # إعطاء صلاحية التنفيذ
      - name: Grant Execute Permission 🔑
        run: chmod +x android/gradlew

      # بناء AAB
      - name: Build Android AAB 🚀
        run: |
          cd android
          ./gradlew bundleRelease

      # إنشاء Tag تلقائي
      - name: Generate Tag 🏷️
        id: tag
        run: echo "tag=build-$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      # إنشاء Release
      - name: Create GitHub Release 🚀
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: AAB Build ${{ steps.tag.outputs.tag }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # رفع AAB إلى Releases
      - name: Upload AAB to Release 📦
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          files: android/app/build/outputs/bundle/release/app-release.aab
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
